/*
** EPITECH PROJECT, 2022
** B-MUL-200-NCE-2-1-myrpg-antoine.frankel
** File description:
** init_entity
*/

#include "rpg.h"

entity *create_entity(wininf *info, int id)
{
    // char **arr = my_strtwa(info->atlases.pokemons_anim[id], ";\n");
    // entity *ne = malloc(sizeof(entity));
    // ne->rect = (sfIntRect){my_atoi(arr[1]), my_atoi(arr[2]),
    //     my_atoi(arr[3]), my_atoi(arr[4])};
    // ne->nb_frames = my_atoi(arr[5]) / ne->rect.width + 1;
    // ne->start_x = ne->rect.left;
    // ne->state = S_WALK;
    // ne->sp = sfSprite_create();
    // sfSprite_setOrigin(ne->sp,
    //     (sfVector2f){ne->rect.width / 2.0f, ne->rect.height / 2.0f});
    // sfIntRect r = (sfIntRect){ne->rect.left, ne->rect.top,
    //     ne->rect.width * 72, ne->rect.height};
    // sfTexture *tex = sfTexture_createFromImage(info->atlases.atlas, &r);
    // sfSprite_setTexture(ne->sp, tex, sfFalse);
    // ne->rect.left = 0;
    // ne->rect.top = 0;
    // sfSprite_setTextureRect(ne->sp, ne->rect);
    // return ne;
}